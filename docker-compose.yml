services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: soloboss
      POSTGRES_USER: soloboss
      POSTGRES_PASSWORD: soloboss
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
